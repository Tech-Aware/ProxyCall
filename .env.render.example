# Variables utilisées par la CLI pour dialoguer avec l'API hébergée sur Render.
# Copiez ce fichier vers .env.render et renseignez les valeurs réelles.

# URL publique Render (ex: https://proxycall.onrender.com)
PUBLIC_BASE_URL=

# Jeton d'accès si l'API Render est protégée par un header ou une auth personnalisée
PROXYCALL_API_TOKEN=

# Paramétrage Twilio utilisé par la CLI en mode LIVE
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_COUNTRY=US
TWILIO_NUMBER_TYPE=mobile
TWILIO_POOL_SIZE=3

# Paramétrage Google Sheets utilisé par la CLI en mode LIVE
GOOGLE_SHEET_NAME=
GOOGLE_SERVICE_ACCOUNT_FILE=/etc/secrets/google-credentials.json
